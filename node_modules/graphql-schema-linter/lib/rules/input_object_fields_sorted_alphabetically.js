"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.InputObjectFieldsSortedAlphabetically = InputObjectFieldsSortedAlphabetically;

var _validation_error = require("../validation_error");

var _listIsAlphabetical = _interopRequireDefault(require("../util/listIsAlphabetical"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function InputObjectFieldsSortedAlphabetically(context) {
  return {
    InputObjectTypeDefinition(node) {
      const fieldList = (node.fields || []).map(field => field.name.value);
      const {
        isSorted,
        sortedList
      } = (0, _listIsAlphabetical.default)(fieldList);

      if (!isSorted) {
        context.reportError(new _validation_error.ValidationError('input-object-fields-sorted-alphabetically', `The fields of input type \`${node.name.value}\` should be sorted alphabetically. ` + `Expected sorting: ${sortedList.join(', ')}`, [node]));
      }
    }

  };
}